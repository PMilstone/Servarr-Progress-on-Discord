# Discord webhook URL (required)
WEBHOOK_URL=https://discord.com/api/webhooks/...

# Port for the webhook server (default 5000)
PORT=5000

# qBittorrent web UI base URL (include http:// or https:// and port if needed)
QB_URL=http://127.0.0.1:8080

# qBittorrent credentials (leave blank if not required)
QB_USER=
QB_PASS=

# Optional message content to send with each embed
MESSAGE=Download status update

# Optional message ID to edit instead of sending new (copy from Discord message link)
MESSAGE_ID=

# Optional interval in seconds to run repeatedly (unset or 0 to run once)
INTERVAL=0